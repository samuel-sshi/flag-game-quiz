name: Keep Supabase alive

on:
  schedule:
    - cron: '0 0 * * 1,4'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -sS -o /dev/null \
            -H "apikey: sb_publishable_SsVccIV5cgoEYyW_e4h8Mw_MkTHSNm6" \
            "https://ymnkzpcjsfzbtfyuihub.supabase.co/rest/v1/scores?select=id&limit=1"
